## 遇到异常进程，异常网络连接
1 使用top查看主机资源利用情况
2 使用ps详细查看异常进程
3 尝试能否找到文件，有一些木马，运行到内存后，会把文件删除

多数木马，病毒，会选择维持权限，把正常的命令替换为恶意命令，要查看/bin目录下时间是否更改

4 kill异常进程，如果过了一会又启动了，1，有定时任务，2，有父进程
5 pstree 查看异常进程父子关系，kill掉父子进程
6 搜索所有定时任务文件，find cron,
用户级别定时任务croontab -l,/var/spool/,
系统级别定时任务/etc/cron.d